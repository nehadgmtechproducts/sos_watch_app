<?xml version="1.0" encoding="utf-8"?>
<!-- Plain background, no logo — matches the app's own dark theme so the OS
     launch window is imperceptible; the in-app SplashScreen widget carries
     the actual branding. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background_color" />
</layer-list>
