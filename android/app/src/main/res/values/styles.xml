<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Uses androidx.core:core-splashscreen for explicit, code-driven splash
         control on every Android version (not just API 31+) — see
         MainActivity.onCreate()'s installSplashScreen() call. Raw
         android:windowSplashScreen* theme attributes alone aren't reliably
         respected on every OEM skin; this compat theme + call is Google's
         recommended path and takes priority over that. Plain matching
         background, no icon — the in-app SplashScreen widget carries the
         actual branding. -->
    <style name="LaunchTheme" parent="@style/Theme.SplashScreen">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="windowSplashScreenBackground">@color/launch_background_color</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_transparent</item>
        <item name="postSplashScreenTheme">@style/NormalTheme</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
